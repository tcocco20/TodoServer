import { Container } from "inversify";
// import { User } from "../exampleModels/user.ts";
// import { Post } from "../exampleModels/post.ts";

export const container = new Container();

// container.bind(User).toSelf().inSingletonScope(); // examples of binding - singleton scope
// container.bind(Post).toSelf().inTransientScope(); // examples of binding - transient scope
